<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Basis-Theme; die eigentlichen Farben liefert Compose (SantaTabTheme). -->
    <style name="Theme.SantaTab" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/santa_bg</item>
    </style>
</resources>
